# SPDX-License-Identifier: GPL-2.0-or-later

set(drawing_unit_SRC
        drawing/drawing.cpp
        drawing/drawing-group.cpp
        drawing/drawing-image.cpp
        drawing/drawing-item.cpp
        drawing/drawing-paintserver.cpp
        drawing/drawing-pattern.cpp
        drawing/drawing-shape.cpp
        drawing/drawing-style.cpp
        drawing/drawing-text.cpp

        # -------
        drawing/drawing.h
        drawing/drawing-group.h
        drawing/drawing-image.h
        drawing/drawing-item.h
        drawing/drawing-paintserver.h
        drawing/drawing-pattern.h
        drawing/drawing-shape.h
        drawing/drawing-style.h
        drawing/drawing-text.h
)

function(get_drawing_unit_lib)
    list(TRANSFORM drawing_unit_SRC PREPEND "renderer/")
    list(PREPEND drawing_unit_SRC
         "util/funclog.cpp"
         "util/pool.cpp"
         "helper/geom.cpp"
    )
    list(TRANSFORM drawing_unit_SRC PREPEND "${CMAKE_SOURCE_DIR}/src/")
    add_library(drawing_unit_lib SHARED "${drawing_unit_SRC}")
    pkg_check_modules(DRAWING_UNIT_DEPS REQUIRED IMPORTED_TARGET cairomm-1.16)
    target_link_libraries(drawing_unit_lib drawing_filters_unit_lib colors_unit_lib libnrtype_unit_lib JPEG::JPEG 2Geom::2geom GLibmm::GLibmm PkgConfig::DRAWING_UNIT_DEPS)
    make_target_unit_testable(drawing_unit_lib)
endfunction(get_drawing_unit_lib)

